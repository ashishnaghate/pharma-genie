# Environment Configuration for PharmaGenie Backend

# Server Configuration
PORT=3000
NODE_ENV=development

# MongoDB Configuration (REQUIRED)
MONGODB_URI=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/
MONGODB_DB_NAME=pharmaGenieDB

# GenAI Provider Configuration
# Options: 'hcl-aicafe' | 'azure-openai' | 'openai' | 'anthropic' | 'mock'
GENAI_PROVIDER=mock

# Model Configuration
GENAI_MODEL=gpt-4o-mini
GENAI_TEMPERATURE=0.7
GENAI_MAX_TOKENS=2000
GENAI_TOP_P=0.95

# HCL AI Cafe Configuration (if using hcl-aicafe)
# HCL_AICAFE_ENDPOINT=https://aicafe.hcl.com/AICafeService/api/v1/subscription/openai
# HCL_DEPLOYMENT_NAME=gpt-4.1
# HCL_API_VERSION=2024-12-01-preview
# GENAI_API_KEY=your-hcl-aicafe-api-key

# Azure OpenAI Configuration (if using azure-openai)
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
# GENAI_API_KEY=your-azure-openai-key

# OpenAI Configuration (if using openai)
# GENAI_API_KEY=sk-your-openai-key

# Anthropic Configuration (if using anthropic)
# GENAI_API_KEY=your-anthropic-key

# Note: Leave GENAI_API_KEY empty to use mock provider (no API calls)
GENAI_API_KEY=
